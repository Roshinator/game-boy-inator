#[cfg(test)]
mod tests
{
    use crate::{ram::*, cpu::*};

    #[test]
    fn ram_write()
    {
        // let mut ram = Ram::new();
        // ram.write(0x0420, 69);
        // assert_eq!(ram.read(0x0420), 69);
    }
}